# GameLauncher 3.5.1 更新

## 📋 简介

GameLauncher 3.5.1 新增游戏云备份功能：将游戏打包为 .vault 归档上传到 123 云盘，随时下载恢复；设置中的自动扫描改为 Steam 式游戏目录机制，下载的游戏自动解压到游戏目录并入库。同时重构了设置页布局、优化了进度条样式，并修复了云备份全链路的多项健壮性问题。

---

## ✨ 新功能特性

### 🗄️ 游戏云备份（123 云盘）
- 详情页新增「归档」按钮：将游戏目录压缩为 `{gid}.vault` 归档上传到 123 云盘
- 超过 5GB 自动分卷（`{gid}.vault` / `{gid}.part2.vault` / ...），每卷带魔数头（GLVLT）与完整性校验
- 重新归档 = 更新备份：先上传全部新分卷，成功后再删除云端旧归档，避免上传失败导致备份丢失
- 上传成功后卡片变为「下载游戏」，可删除本地文件释放空间，随时一键恢复
- 下载自动解压到游戏目录并入库，按 gid 去重（不会产生重复卡片）
- 归档文件损坏（魔数/大小/分卷序号不符）时提示并中止，不留下半成品游戏目录
- 需在 123 开放平台创建应用获取 clientID / clientSecret，设置页获取访问令牌（凭据与令牌 DPAPI 加密存储，令牌过期自动续期）

### 📂 游戏目录机制（Steam 式）
- 设置中的「自动扫描」改为「游戏目录」：指定一个目录，启动时自动识别其中的 .gmd 游戏
- 云备份下载的游戏默认解压到该游戏目录，保持原目录结构（`游戏目录\原文件夹名\游戏文件`）

### 🎨 界面与体验
- 详情页按钮重新排版为两行布局（删除 / 打开路径 / 编辑 / 归档 / 启动）
- 上传 / 下载过程中按钮变为进度条：白色底 + 绿色从左往右填充 + 转圈动效 + 进度文字
- 设置页按分类重新布局（通用 / 云同步 / 云备份 / 数据管理 / 高级）
- Toast 分级配色：成功绿色 / 警告橙色 / 错误红色，不再全部显示为错误红

---

## 🐛 修复

- **归档上传确认**：修复「文件正在校验中 (code=20103)」被误判为失败，改为异步轮询等待校验完成
- **分片上传**：修复并发上传时循环闭包导致的分片 MD5 缺失
- **下载恢复**：修复下载文件被占用导致校验失败；修复下载完成后卡片按钮未变回「启动游戏」
- **进度条**：修复归档/下载完成后卡片进度条卡住（Done 阶段重复写入传输状态）
- **游戏目录保存**：修复关闭设置对话框时路径被空值覆盖的问题
- **云端归档目录**：修复同名归档文件夹重复创建失败（复用已有目录）
- **安全**：修复下载解压路径穿越可删除任意目录的隐患；access_token 改为 DPAPI 加密存储
- **可靠性**：修复 HTTP 重试复用已发送请求导致失败；空目录归档不再产生无效备份；恢复入库保留原目录名

---

