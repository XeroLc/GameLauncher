# GameLauncher 3.2 正式版发布 🎉

## 📋 简介

GameLauncher 3.2 重点修复了 5 个关键 Bug 和 5 项性能问题，同时完成了架构重构，引入依赖注入容器，提升代码质量和系统稳定性。

---

## ✨ 新功能特性

### 🔴 Bug 修复
- **图片加载死锁**：异步保存不再阻塞 UI 线程消息循环
- **磁盘扫描效率**：黑名单目录在入口跳过，不再遍历海量文件后丢弃
- **服务实例复用**：AutoScan 循环内服务只创建一次
- **N+1 查询优化**：收藏夹管理使用 GROUP BY 单次查询
- **竞态条件修复**：窗口激活添加原子检查防止并发冲突

### ⚡ 性能优化
- 图片转换代码提取为共享工具方法，消除重复
- 时间格式化逻辑三处统一为一个静态方法
- 游戏卡片统计改为按需更新，减少 UI 遍历
- 数据同步签名改为 COUNT+MAX 轻量级检查
- 修复 GMD 文件锁 ConcurrentDictionary 内存泄漏

### 🏗️ 架构重构
- 重构 UpdateChecker 静态状态管理为实例模式，支持线程安全
- 引入 Microsoft.Extensions.DependencyInjection 依赖注入容器
- MainWindow 通过 partial class 拆分为多个文件
- 提取 GameImageLoader 独立服务类
- 删除冗余 DatabaseInitializer 类

### 🆔 游戏唯一标识符系统 (GID)
- 为每个游戏分配独立唯一 ID
- 采用 GID+9位数字固定格式（如 GID594370512）
- 使用密码学安全随机数生成，最多重试5次确保唯一性
- 支持跨数据库的唯一识别与关联
- 数据库自动迁移，为旧游戏自动补充 GID

### 📦 GMD 文件管理优化
- 统一使用游戏 GID 作为 GMD 文件名
- 设置页面新增"清理旧 GMD 文件"功能按钮
- 自动扫描并删除不符合新命名规范的旧文件

### 🚀 GMD 文件选择流程优化
- 仅保留"选择 GMD 文件"单一按钮
- 选择完成后一键完成解析→分配GID→入库→关闭对话框
- 去除原有的二次确认步骤

### 📁 资源文件管理改进
- GMD 导入时自动创建专用资源文件夹
- 图标和预览图统一存储到专用文件夹
- 彻底移除对临时文件夹的依赖

### 🔒 功能权限控制调整
- GMD 文件导入功能仅在"添加游戏"页面可用
- 编辑游戏页面隐藏 GMD 导入入口

### 🎨 全新云雾磨砂玻璃 UI 设计语言
- 窗口背景升级为磨砂玻璃效果
- 游戏卡片采用半透明磨砂质感
- 云雾渐变装饰层营造氛围感
- 弹窗全面升级磨砂玻璃风格
- 标签采用半透明磨砂胶囊设计
- 按钮升级磨砂质感交互
- 深色/浅色模式完美适配

### 🔄 GitHub 更新检查
- 启动时自动检查 GitHub 最新版本
- 检测到新版本时弹出更新提示对话框
- 版本日志窗口增加手动检查更新按钮
- 支持 API/Atom Feed/HTML 三级回退检测机制

### ⭐ 游戏收藏与检索功能
- 支持收藏游戏并快速访问收藏列表
- 增强游戏搜索与标签筛选能力
- 标签系统交互体验优化

---

## 🔧 技术改进

- 新增 UpdateCheckerService 更新检查服务
- GitHub API 三级回退：api.github.com → releases.atom → HTML 页面抓取
- 优化 ContentDialog 管理，避免多对话框冲突
- 优化磨砂玻璃渲染性能

---

## 📊 系统要求

- Windows 10 1809 (10.0.17763.0) 或更高版本
- Windows 11
- 64 位系统 (x64)
- Windows App SDK 1.8 运行时

---

## 🚀 安装说明

### 1. 安装 Windows App SDK 运行时
下载并安装：https://aka.ms/windowsappsdk/1.8/windowsappruntimeinstall-x64.exe

### 2. 运行 GameLauncher
1. 下载并解压压缩包
2. 双击 `GameLauncher.exe` 启动程序
3. （可选）创建快捷方式到桌面

---

## 📝 更新日志完整记录

### v3.2 (2026-05-13) - 稳定与性能
- 🔴 **5 项 Bug 修复** - 死锁风险、磁盘扫描效率、N+1 查询、竞态条件等关键问题
- ⚡ **5 项性能优化** - 图片转换复用、时间格式化统一、签名计算轻量化、内存泄漏修复
- 🏗️ **架构重构** - 引入 DI 容器，UpdateChecker 实例化，MainWindow 拆分，GameImageLoader 提取

### v3.1 (2026-05-13) - 数据管理增强
- 🆔 **游戏唯一标识符系统** - 每个游戏分配独立 GID，采用 GID+9位数字格式
- 📦 **GMD 文件管理优化** - 统一 GID 命名规范，新增清理旧格式文件功能
- 🚀 **GMD 文件选择流程优化** - 选择后一键自动完成添加
- 📁 **资源文件管理改进** - 自动创建专用文件夹，移除临时目录依赖
- 🔒 **功能权限控制** - GMD 导入仅在添加页面可用

### v3.0 (2026-05-13) - UI 全面升级
- 🎨 **全新云雾磨砂玻璃 UI 设计语言** - 窗口背景、游戏卡片、弹窗、标签、按钮全面升级磨砂质感
- 🌗 **深色/浅色模式完美适配** - 云雾渐变装饰层营造氛围感
- 🔄 **GitHub 更新检查** - 启动时自动检查最新版本，支持手动检查，API/Atom Feed/HTML 三级回退
- ⭐ **游戏收藏与检索功能** - 支持收藏游戏并快速访问，增强搜索与标签筛选能力

### v2.1.1 (2026-02-24)
- 黑暗模式弹窗适配
- 标签下拉菜单优化
- 游戏卡片高亮修复
- 弹窗滚动功能

### v2.1 (2026-02-23)
- 游戏详情页编辑功能
- 编辑按钮布局优化
- 编辑后数据实时更新

### v2.0 (2026-02-21) - 正式版大更新
- 🎨 **全新 UI/UX 设计** - 重新设计的游戏卡片和界面布局，更加现代美观
- 🌙 **深色/浅色主题切换** - 新增主题切换功能，支持深色和浅色两种主题
- 📈 **游戏排序功能** - 支持多种排序方式：名称、启动次数、游戏时长、添加时间、最近运行
- ⭐ **收藏夹功能** - 数据库已支持 IsFavorite 字段，为未来的收藏夹功能做好准备
- ✨ **优化游戏卡片** - 卡片尺寸调整为 240x340，布局更合理美观
- 📊 **运行状态指示器** - 改进运行状态显示，更直观的运行中标记
- 🐛 **修复标签问题** - 修复了标签显示为图片路径的严重 Bug

### v1.8.1 (2026-02-20)
- 🐛 修复标签输入框第一次点击不显示下拉框的问题
- ✨ 优化标签选择体验

### v1.8 (2026-02-20)
- ✨ 新增标签系统，支持为游戏添加自定义标签
- ✨ 新增标签筛选功能，可通过标签快速分类和查找游戏
- ✨ 增强搜索功能，现在支持通过标签搜索游戏
- ✨ 在游戏详情页面显示标签
- 🐛 优化用户体验

---

## 🎯 未来计划

- 侧边栏导航
- 更多主题选项
- 游戏分组功能
- 云同步功能

---

## 📄 作者信息

- **作者**：@XeroLc
- **项目地址**：https://github.com/XeroLc/GameLauncher
- **版本**：3.2 正式版
- **发布日期**：2026-05-13